apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "echo-server.fullname" . }}-config
  labels:
    {{- include "echo-server.labels" . | nindent 4 }}
data:
  {{- with .Values.config }}
    {{- toYaml . | nindent 4}}
    {{- end}}